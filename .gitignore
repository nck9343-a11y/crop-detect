# ---- 数据集 ------------------------------------------------
# 7.5 GB，且主数据集的再分发许可尚未确认。
# 获取与重建方式见 datasets/README.md
datasets/*
!datasets/README.md
!datasets/README.en.md

# ---- 模型权重 ----------------------------------------------
# 单文件 116 MB–462 MB，超过 GitHub 100 MB 的单文件硬限制
weights/
runs/
results/runs_rfdetr/

# ---- 安装包 ------------------------------------------------
# torch/torchvision 的本地 whl，1.8 GB；安装方式见 requirements.txt
installers/
*.whl

# ---- Python ------------------------------------------------
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# ---- 编辑器与系统 ------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
~$*

# ---- 论文生成物 --------------------------------------------
# docx 由 scripts/Md_to_docx.py 从 论文.md 生成，不入库以免二进制冲突；
# 投稿定稿那一版可以 git add -f 强制加入
paper/*.docx
paper/drafts/*.docx

# ---- 自采照片 ----------------------------------------------
# 本地保留，不入库：这批照片未支撑任何已发表结果（论文 4.2 节已将其
# 排除在正式实验之外），而仓库为公开，故不随之公开。
my_grape/

# ---- 内部工作文档 ------------------------------------------
# 本地保留，不入库：投稿待办与田间采集规程都是写给作者本人的工作记录
# （第二人称语气），不支撑任何已发表结果，不属于研究产物。
paper/submission_checklist.md
paper/field_collection_protocol.md

# ---- 历史草稿 ----------------------------------------------
# 本地保留，不入库：git 历史已取代手工留版本，
# 而这些草稿含已更正的数字与已撤回的说法，不宜进入永久历史
paper/drafts/
